# Production Environment Variables
# Rename to .env.production and fill with actual values
# NEVER commit this file to version control

# Django - Production Security
# Generate a strong secret key: openssl rand -base64 64
SECRET_KEY=your-strong-production-secret-key-generated-with-openssl-rand-base64-64
DEBUG=False
ALLOWED_HOSTS=your-production-domain.com,www.your-production-domain.com,server-ip-address
PRODUCTION_HOSTS=${ALLOWED_HOSTS}  # Used in docker-compose.production.yml
MANAGERS="Admin Name <admin@your-domain.com>"
ADMINS="Tech Lead <tech@your-domain.com>"
SERVER_EMAIL=noreply@your-domain.com
DEFAULT_FROM_EMAIL=noreply@your-domain.com

# Database - Production
# Use strong, random passwords: openssl rand -base64 32
POSTGRES_USER=sportsclub_prod
POSTGRES_PASSWORD=strong-random-postgres-password-generated-with-openssl-rand-base64-32
POSTGRES_DB=sportsclub_production
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Docker Registry (for task 5)
DOCKER_USERNAME=s4lvaborjamoll
# DOCKER_PASSWORD should be set as environment variable, not in this file

# Blue-Green Deployment
# Active color for blue-green deployment (blue or green)
ACTIVE_COLOR=blue
# App version tag (should match Docker image tag)
APP_VERSION=latest